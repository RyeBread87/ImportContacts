package com.example.importcontacts.clicklisteners;

import com.example.importcontacts.models.Contact;

// interface for
public interface CustomContactClickListener {
    void editContact(Contact contact);
}
